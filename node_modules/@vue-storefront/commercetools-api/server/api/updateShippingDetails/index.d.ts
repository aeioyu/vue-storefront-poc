import { CustomQuery } from '@vue-storefront/core';
import { CartResponse } from '../../types/Api';
import { Cart, Address } from '../../types/GraphQL';
declare const updateShippingDetails: (context: any, cart: Cart, shippingDetails: Address, customQuery?: CustomQuery) => Promise<CartResponse>;
export default updateShippingDetails;
