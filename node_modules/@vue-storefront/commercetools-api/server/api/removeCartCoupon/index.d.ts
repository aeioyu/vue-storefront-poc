import { CustomQuery } from '@vue-storefront/core';
import { CartResponse } from '../../types/Api';
import { Cart, ReferenceInput } from '../../types/GraphQL';
declare const removeCartCoupon: (context: any, cart: Cart, discountCode: ReferenceInput, customQuery?: CustomQuery) => Promise<CartResponse>;
export default removeCartCoupon;
