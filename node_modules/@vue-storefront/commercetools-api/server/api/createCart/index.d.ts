import { CartData } from './../../types/Api';
import { CustomQuery } from '@vue-storefront/core';
declare const createCart: (context: any, cartDraft?: CartData, customQuery?: CustomQuery) => Promise<any>;
export default createCart;
